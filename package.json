{
  "name": "leetcode-coach",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "coach": "src/cli/coach.js"
  },
  "scripts": {
    "build:server": "esbuild src/server/index.js --bundle --platform=node --format=esm --outfile=dist/server.js --external:better-sqlite3 --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && cp src/db/schema.sql dist/schema.sql",
    "start": "npm run build:server && node dist/server.js",
    "setup": "npm install && npm install --prefix dashboard && npm run build --prefix dashboard",
    "dev": "node scripts/dev.js",
    "build:ui": "npm run build --prefix dashboard",
    "screenshot": "node scripts/screenshot.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.106.0",
    "better-sqlite3": "^11.0.0",
    "commander": "^12.0.0",
    "express": "^4.19.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "jsdom": "^24.1.3",
    "playwright": "^1.61.1",
    "supertest": "^7.0.0",
    "three": "^0.160.0",
    "vitest": "^2.0.0"
  }
}
